from django.conf.urls.defaults import *

prefix = '{module_name}'

urlpatterns = patterns('{package_name}.views',
    url(r'^$','{module_name}_view',{{'module_name':'{module_name}'}},name='{module_name}-main'),
)
