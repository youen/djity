#!/usr/bin/env python

from setuptools import setup,find_packages

setup(name='djity-{module_name}',
    version='0.1',
    description='{description}',
    author='{author_name}',
    author_email='{author_email}',
    url='{url}',
    packages=['{package_name}'],
    package_data={{'{package_name}':[
		'templates/{package_name}/*.html',
		'media/*/*']
		}},
)
